version: "3.8"

services:
  pursuit-framework:
    build: .
    container_name: pursuit-framework
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      STREAMLIT_SERVER_PORT: 8501
      STREAMLIT_SERVER_ADDRESS: 0.0.0.0
    restart: unless-stopped